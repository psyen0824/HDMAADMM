PKG_CXXFLAGS += $(shell "${R_HOME}/bin/Rscript" -e "oneMKL::onemklIncFlags()") $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS += $(shell "${R_HOME}/bin/Rscript" -e "oneMKL::onemklLibFlags()") $(SHLIB_OPENMP_CXXFLAGS)
